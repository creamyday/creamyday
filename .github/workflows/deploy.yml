name: Build and Deploy to GitHub Pages

on:
  push:
    branches: [ main ] # 當你 push 到 main 分支時觸發

permissions:
  contents: write # 賦予 Actions 寫入分支的權限

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Install and Build 🔧
        run: |
          npm install
          npm run build

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist # 部署 Vite 編譯後的資料夾
          branch: gh-pages # 指定部署到 gh-pages 分支
          clean: true # 每次部署前自動清理
